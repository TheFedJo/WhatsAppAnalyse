/**
 * The type of message (self-explanatory imo)
 */

public enum MessageType {
    ADD,
    CODE_CHANGE,
    EDIT_DESCRIPTION,
    EDIT_PHOTO,
    GENESIS,
    JOIN,
    KICK,
    LEAVE,
    OTHER,
    STANDARD
}